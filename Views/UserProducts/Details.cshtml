@* @model BoxBuildproj.Models.Productstbl *@

@* @{ *@
@*     ViewData["Title"] = Model.ProductName; *@
@* } *@

@* <h2>@Model.ProductName</h2> *@

@* <div class="card"> *@
@*     <img src="@Model.ImagePath" class="card-img-top" alt="Product Image" style="max-height: 400px; object-fit: cover;"> *@
@*     <div class="card-body"> *@
@*         <h5 class="card-title">@Model.ProductName</h5> *@
@*         <p class="card-text">@Model.Description</p> *@
@*         <p class="card-text"><strong>Price:</strong> $@Model.Price</p> *@
@*         <a asp-controller="Cart" asp-action="AddToCart" asp-route-productId="@Model.ProductID" class="btn btn-success">Add to Cart</a> *@
@*         <a asp-controller="Wishlist" asp-action="AddToWishlist" asp-route-productId="@Model.ProductID" class="btn btn-danger">Add to Wishlist</a> *@
@*     </div> *@
@* </div> *@

@* <a asp-action="Index" class="btn btn-secondary mt-3">Back to Products</a> *@


@model BoxBuildproj.Models.Productstbl

@{
    ViewData["Title"] = Model.ProductName;
}
<!doctype html>
<html lang="zxx">

<head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>BoxBuild</title>
    <link rel="icon" href="~/pillowmart-master/img/favicon.png">
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="~/pillowmart-master/css/bootstrap.min.css">
    <!-- animate CSS -->
    <link rel="stylesheet" href="~/pillowmart-master/css/animate.css">
    <!-- owl carousel CSS -->
    <link rel="stylesheet" href="~/pillowmart-master/css/owl.carousel.min.css">
    <!-- font awesome CSS -->
    <link rel="stylesheet" href="~/pillowmart-master/css/all.css">
    <!-- flaticon CSS -->
    <link rel="stylesheet" href="~/pillowmart-master/css/flaticon.css">
    <link rel="stylesheet" href="~/pillowmart-master/css/themify-icons.css">
    <!-- font awesome CSS -->
    <link rel="stylesheet" href="~/pillowmart-master/css/magnific-popup.css">
    <!-- swiper CSS -->
    <link rel="stylesheet" href="~/pillowmart-master/css/slick.css">
    <!-- style CSS -->
    <link rel="stylesheet" href="~/pillowmart-master/css/style.css">


<style>
    .product-title {
        font-size: 28px;
        font-weight: bold;
    }

    .product-price {
        font-size: 22px;
        font-weight: bold;
    }

    .product-image {
        position: relative;
        text-align: center;
    }

    .main-product-img {
        max-width: 100%;
        height: 400px;
        object-fit: contain;
        cursor: zoom-in;
        transition: transform 0.3s ease-in-out;
    }

        .main-product-img:hover {
            transform: scale(1.2);
        }

    .thumbnail-container {
        display: flex;
        justify-content: center;
        gap: 10px;
    }

    .thumbnail-img {
        width: 60px;
        height: 60px;
        object-fit: cover;
        cursor: pointer;
        border: 2px solid #ddd;
        transition: 0.3s ease-in-out;
    }

        .thumbnail-img:hover {
            border-color: #007bff;
        }

        .main-product-img {
            transition: transform 0.3s ease;
        }

            .main-product-img:hover {
                transform: scale(1.05);
            }

</style>

<script>
        function changeImage(thumbnail) {
        document.getElementById("mainImage").src = thumbnail.src;
    }

</script>
</head>
<body>
    <header style="background: rgba(255, 255, 255, 0.85); backdrop-filter: blur(10px); box-shadow: 0 8px 24px rgba(0,0,0,0.1);">
        <div class="container py-3">
            <nav class="navbar navbar-expand-lg navbar-light">
                <!-- Logo -->
                <a class="navbar-brand d-flex align-items-center" asp-controller="User" asp-action="Home">
                    <img src="~/pillowmart-master/img/logobox.png" asp-append-version="true" alt="logo" style="height: 45px;" />
                </a>

                <!-- Toggler for mobile -->
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <!-- Nav items -->
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav align-items-center ms-auto" style="gap: 30px; font-weight: 500; font-size: 16px;">
                        <li class="nav-item">
                            <a class="nav-link link-animate" asp-controller="User" asp-action="Home">Home</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link link-animate" asp-controller="User" asp-action="About">About</a>
                        </li>

                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle link-animate" href="#" role="button" data-bs-toggle="dropdown">Product</a>
                            <div class="dropdown-menu dropdown-animated shadow rounded">
                                <a class="dropdown-item" asp-controller="UserProducts" asp-action="Product_list">Product List</a>
                            </div>
                        </li>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle link-animate" href="#" role="button" data-bs-toggle="dropdown">Pages</a>
                            <div class="dropdown-menu dropdown-animated shadow rounded">
                                @if (User.Identity.IsAuthenticated)
                                {
                                    <span class="dropdown-item text-muted">Hi, @User.Identity.Name</span>
                                    <a class="dropdown-item" asp-controller="Profile" asp-action="Index">My Profile</a>
                                    <a class="dropdown-item" asp-controller="Checkout" asp-action="Index">Checkout</a>
                                    <a class="dropdown-item" asp-controller="Cart" asp-action="Index">Cart</a>
                                    <a class="dropdown-item" asp-controller="Order" asp-action="MyOrders">My Orders</a>
                                }
                                else
                                {
                                    <a class="dropdown-item" asp-area="Identity" asp-page="/Account/Login">Login</a>
                                    <a class="dropdown-item" asp-area="Identity" asp-page="/Account/Register">Register</a>
                                }
                            </div>
                        </li>

                        @if (User.Identity.IsAuthenticated)
                        {
                            <li class="nav-item">
                                <form method="post" asp-area="Identity" asp-page="/Account/Logout" class="d-inline">
                                    @Html.AntiForgeryToken()
                                    <button type="submit" class="btn btn-sm btn-outline-danger">Logout</button>
                                </form>
                            </li>
                        }


                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle link-animate" href="#" role="button" data-bs-toggle="dropdown">Blog</a>
                            <div class="dropdown-menu dropdown-animated shadow rounded">
                                <a class="dropdown-item" asp-controller="Blog" asp-action="Index">Blog</a>
                                <a class="dropdown-item" asp-controller="Blog" asp-action="SingleBlog">Single Blog</a>
                            </div>
                        </li>

                        <li class="nav-item">
                            <a class="nav-link link-animate" asp-controller="Contact" asp-action="Index">Contact</a>
                        </li>

                        <!-- Icons -->

                        <li class="nav-item ms-2">
                            <a asp-controller="Cart" asp-action="Index" class="text-dark icon-animate" style="font-size: 20px;">
                                <i class="flaticon-shopping-cart-black-shape"></i>
                            </a>
                        </li>
                    </ul>
                </div>
            </nav>
        </div>
    </header>

    <div class="container mt-5">
        <div class="row bg-white p-4 shadow rounded">
            <!-- Product Image Section -->
            <div class="col-md-6 d-flex justify-content-center align-items-center">
                <div class="product-image p-3 border rounded">
                    <img src="@Model.ImagePath" alt="@Model.ProductName" class="img-fluid rounded main-product-img" style="max-height: 400px;" />
                </div>
            </div>

            <!-- Product Details Section -->
            <div class="col-md-6">
                <h2 class="fw-bold">@Model.ProductName</h2>
                <p class="text-muted fs-5">@Model.Description</p>
                @{
                    var offer = Model.ProductOffer?.FirstOrDefault()?.Offer;
                    decimal finalPrice = Model.Price;

                    if (offer != null && offer.DiscountPercentage > 0)
                    {
                        finalPrice = Model.Price - (Model.Price * offer.DiscountPercentage / 100);
                    }
                }

                @if (offer != null && offer.DiscountPercentage > 0)
                {
                    <h4 class="text-success fw-semibold mb-1">
                        ₹@finalPrice.ToString("0.00")
                        <small class="text-muted ms-2"><del>₹@Model.Price.ToString("0.00")</del></small>
                    </h4>
                    <span class="badge bg-danger mb-3">Save @offer.DiscountPercentage% - @offer.Title</span>
                }
                else
                {
                    <h4 class="text-success fw-semibold mb-3">₹@Model.Price.ToString("0.00")</h4>
                }



                <!-- Quantity Selector -->
                <div class="d-flex align-items-center mb-3">
                    <label for="quantity" class="me-2 fw-semibold">Quantity:</label>
                    <select id="quantity" class="form-select w-auto shadow-sm">
                        @for (int i = 1; i <= 10; i++)
                        {
                            <option value="@i">@i</option>
                        }
                    </select>
                </div>

                <!-- Add to Cart & Wishlist Buttons -->
                <div class="d-flex gap-3 mt-4">
                    <form asp-controller="Cart" asp-action="AddToCart" method="post" class="d-flex gap-3 mt-4">
                        <input type="hidden" name="productId" value="@Model.ProductID" />
                        <input type="hidden" name="price" value="@finalPrice" />
                        <div>
                            <select id="quantity" name="quantity" class="form-select w-auto shadow-sm">
                                @for (int i = 1; i <= 10; i++)
                                {
                                    <option value="@i">@i</option>
                                }
                            </select>
                        </div>
                        <button type="submit" class="btn btn-primary btn-lg px-4 shadow">
                            <i class="fas fa-shopping-cart me-1"></i> Add to Cart
                        </button>
                        <a asp-controller="Wishlist" asp-action="AddToWishlist" asp-route-productId="@Model.ProductID" class="btn btn-outline-danger btn-lg px-4 shadow">
                            <i class="fas fa-heart me-1"></i> Wishlist
                        </a>
                    </form>

                   
                </div>

                <a asp-action="Product_list" class="btn btn-link mt-4 text-decoration-none"><i class="fas fa-arrow-left me-1"></i> Back to Products</a>
            </div>
        </div>
    </div>

    @if (ViewBag.RecentlyViewed != null)
    {
        <h3 class="mt-5">Recently Viewed</h3>
        <div class="row">
            @foreach (var item in (List<BoxBuildproj.Models.Productstbl>)ViewBag.RecentlyViewed)
            {
                <div class="col-md-3">
                    <div class="card mb-3">
                        <img src="@item.ImagePath" class="card-img-top" alt="@item.ProductName">
                        <div class="card-body">
                            <h5 class="card-title">@item.ProductName</h5>
                            <p class="card-text">₹@item.Price</p>
                            <a href="@Url.Action("Details","UserProducts", new { id = item.ProductID })" class="btn btn-primary btn-sm">View</a>
                        </div>
                    </div>
                </div>
            }
        </div>
    }

                </body>
                </html>